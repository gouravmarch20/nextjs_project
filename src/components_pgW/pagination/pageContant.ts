const PREV_NEXT = 3;
const MAX_PAGE_NO = 100;
const NEXT_LIMIT = 4;

export type ImageType = {
    id: string;
    author: string;
    download_url: string;
  };
  
export { PREV_NEXT, MAX_PAGE_NO, NEXT_LIMIT  };
